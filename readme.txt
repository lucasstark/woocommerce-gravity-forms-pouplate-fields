To configure the field for dynamic population set it's Allow field to be populated dynamically setting to True.

For the Parameter Name enter in:  wc_attribute_your_product_attribute_name.

For example, if you had a size product attribute you could enter in wc_attribute_pa_size.

Note - This is currently only designed for select fields.
